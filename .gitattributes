# Handle line endings automatically for files detected as text
# and leave all files detected as binary untouched.
* text=auto

# Force text files to have LF line endings in the repository
*.dart text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
*.md text eol=lf
*.json text eol=lf

# Flutter web build files - treat as binary to avoid line ending conversion
/build/web/* binary
canvaskit/* binary
*.js binary
*.js.symbols binary
